name: Get Runner IP
on: [workflow_dispatch]
jobs:
  ip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Public IP: $(curl -s https://api.ipify.org)"
      - run: echo "Local IP: $(hostname -I | cut -d' ' -f1)"
      - run: sleep 10